{
  "name": "@settle/payzone",
  "version": "0.0.1",
  "description": "Payzone gateway for Settle",
  "main": "lib/index.js",
  "scripts": {
    "build": "rimraf lib && tsc",
    "format": "prettier --write \"**/*.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "supertest": "^4.0.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.3"
  }
}
